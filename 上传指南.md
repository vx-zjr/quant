# 上传到GitHub指南

请在本地Windows或Mac上执行以下命令：

## 1. 初始化Git（如果还未初始化）
```bash
cd h:\quant_kn
git init
git remote add origin https://github.com/vx-zjr/quant.git
```

## 2. 添加并提交所有文件
```bash
git add .
git commit -m "整理AI量化交易知识库 - 2026-05-02"
```

## 3. 推送到GitHub
```bash
git push -u origin master
```

## 4. 如果远程仓库已有文件
```bash
git pull origin master --allow-unrelated-histories
git push -u origin master
```

---

**仓库地址**: https://github.com/vx-zjr/quant

**执行后即可在GitHub上查看整理后的知识库**
